# ft_transcendence

BACKEND: npx @nestjs/cli new . -> crea todos los archivos package.json, tsconfig.json, etc. Lo necesario para usar nestJS.

cd backend
npm install prisma @prisma/client -> CLI para manejar el esquema y migraciones y el cliente que se usara en NestJS

FRONTEND: npm create vite@latest -> frontend en reac y se utiliza 'Vite' para "localhost:5173" y "npm run build"

npm install prisma @prisma/client
npx prisma init